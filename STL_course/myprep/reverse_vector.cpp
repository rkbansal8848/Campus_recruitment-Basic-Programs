#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main() {
    // vector <char> v={'a','b','c'};
    // reverse(v.begin(),v.end());
    // for(char& a:v){
    //     cout<<a;
    //  }

    char a[]={'a','b','c'};
    reverse(a.begin(),a.end());
return 0;
}